//esta es la vista principal de la aplicación, que contiene el layout y el contenido de la página
//cargados desde el componente AppLayout, que se encuentra en la carpeta components/layout :)))
import AppLayout from "./components/layout/AppLayout.jsx";
import { useState } from "react";
function App() {
    
    return (
        <AppLayout />

    );
    
}

export default App;